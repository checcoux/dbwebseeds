<%= form_for(@column) do |f| %>
  <% if @column.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@column.errors.count, "error") %> prohibited this column from being saved:</h2>

      <ul>
      <% @column.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ordine %><br>
    <%= f.number_field :ordine %>
  </div>
    <div class="field">
      <%= f.label :larghezza %><br>
      <%= f.number_field :larghezza %>
    </div>
  <div class="field">
    <%= f.label :contenuto %><br>
    <%= f.cktext_area :contenuto %>
  </div>
    <div class="field">
      <%= f.label :row_id %><br>
      <%= f.collection_select(:row_id, Row.all, :id, :id) %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
