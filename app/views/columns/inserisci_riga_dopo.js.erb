$('#row<%= @row.id %>').replaceWith("<%= escape_javascript render(@row) + render(@row2) %>").foundation();
$('#row<%= @row.id %> > .columns').addClass('bordo_layout');
$('#row<%= @row2.id %> > .columns').addClass('bordo_layout');

$('#row<%= @row.id %> .mioslider').slick({
  slidestoshow: 1,
  slidestoscroll: 1,
  autoplay: true,
  autoplayspeed: 2000,
  zIndex: 100,
  arrows: true
});